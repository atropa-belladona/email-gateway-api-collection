info:
  name: Messages
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/messages"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "to" : [
          "blackshields99@gmail.com"
          ], 
        "subject": "test dulu aja",
        "html_body": "<b>Test Bold</b>"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
