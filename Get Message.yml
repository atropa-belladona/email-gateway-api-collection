info:
  name: Get Message
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/messages/:id"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  params:
    - name: id
      value: 019e09e6-668c-7243-a162-a2d2d4cfc3e3
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
